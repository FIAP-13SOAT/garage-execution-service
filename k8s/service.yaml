apiVersion: v1
kind: Service
metadata:
  name: garage-execution-service
  namespace: default
spec:
  selector:
    app: garage-execution-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
