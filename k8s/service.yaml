apiVersion: v1
kind: Service
metadata:
  name: garage-execution-service
  namespace: default
  labels:
    app: garage-execution-service
spec:
  selector:
    app: garage-execution-service
  ports:
    - protocol: TCP
      port: 8084
      targetPort: 8084
  type: ClusterIP
